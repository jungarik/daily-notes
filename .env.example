# Get a token from @BotFather on Telegram
BOT_TOKEN=123456:ABC-your-token-here

# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/notes

# OpenAI API key for generating chunk embeddings (semantic search)
OPENAI_API_KEY=sk-your-key-here

# Google Speech-to-Text API key (for transcribing voice messages)
GOOGLE_STT_API_KEY=your-google-api-key
# Primary language Google should expect in voice notes (BCP-47), e.g. uk-UA, en-US
STT_LANGUAGE_CODE=uk-UA
# Extra languages to auto-detect (comma-separated, up to 3), e.g. en-US
STT_ALTERNATE_LANGUAGES=en-US

# Timezone used to resolve reminder times like "tomorrow at 9" (IANA name)
REMINDER_TZ=Europe/Kyiv
# Model used only when rule-based reminder parsing can't pin a time
REMINDER_LLM_MODEL=gpt-4o-mini
